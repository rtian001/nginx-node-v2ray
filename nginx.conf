server {
    listen 80;
    root /usr/share/nginx/html;
    location / {
        try_files $uri $uri/ /index.html; 
    }
    # 代理 Node API (示例)
    location /api {
        proxy_pass http://localhost:3000;
    }
}
